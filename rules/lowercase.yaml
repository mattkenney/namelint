default_missing_value: "false"
default_value: "false"
description: |
  Base file name (i.e. without extension) must be lowercase ASCII alphanumeric.
handle: "lowercase"
help: "Lowercase file names"
regex:
  pattern: "^[a-z0-9.]+([.][a-zA-Z0-9]+)?$"
  case_insensitive: false
  result: "pass"
title: "Lowercase File Names"
