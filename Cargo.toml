[package]
name = "namelint"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.16", default-features = false, features = ["help", "usage", "error-context", "suggestions", "derive", "env", "std"] }
regex = "1.10.6"
