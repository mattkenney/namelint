#
# this is the config file for namelint itself
#
dirs: [ "." ]
ignore-dirs:
  - ".git"
  - ".jekyll-cache"
  - "target"
  - "_site"
lints:
  - lint_id: self
    paths:
      - "**/*"
    rules:
      - no-msdos-reserved
      - no-whitespace
      - posix-portable
